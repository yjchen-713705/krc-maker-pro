
### 关于项目初始化

1.  **是否已经初始化过项目？**
    - **目录状态**：`/Users/yijingchen/dev/krc-maker` 目前是一个**完全空目录**，没有任何文件。项目初始化工作可以完全交给AI助手从头开始。
    - **包管理器**：用 `pnpm` 作为包管理器。

### 关于技术栈的细节

2.  **Electron 启动方式**：
    - **我推荐使用 `electron-vite`**。你提到的理由非常充分，它配置简洁，是Vite生态下Electron开发的事实标准方案。就按这个来。

3.  **歌词处理库 `@jyostudio/lyric`**：
    - **这是一个核心依赖，请信任并使用它**。这个库的作者在歌词解析领域经验丰富，其API设计就是为了处理KRC这类复杂格式。我们应该**完全依赖它**来完成KRC的解析、生成和实时控制（`LyricController`），除非后续发现确切的API不满足需求，否则不自行实现底层逻辑。
    - 建议AI助手在集成前，先快速查阅该库的README或类型定义(`.d.ts`)，了解关键API（如 `Lyric.createFromUri`、`Lyric.generate`、`LyricController` 的 `state` 和 `update` 方法等）。

4.  **UI 组件库**：
    - 选择 **Ant Design (antd)**，理由和你想的完全一致：中文文档成熟、组件丰富（`Slider`, `Table`, `Input`, `Button` 等），能极大加速开发。

### 关于功能设计的细节

5.  **逐字打轴 vs 逐句打轴的切换**：
    - **两者都支持**。交互方式为：**提供一个明显的模式切换开关（如Toggle按钮）**，让用户手动切换当前是“逐字模式”还是“逐句模式”。
    - 在“逐字模式”下，`→`键标记单个字；在“逐句模式”下，`Enter`键标记整句（注：`Enter`在两种模式下均可标记整句，这样更符合直觉）。

6.  **音频文件的加载**：
    - **两者都支持**。优先实现“**打开文件**”对话框，同时支持**将音频文件拖拽到应用窗口**进行加载。

7.  **歌词编辑的细节**：
    - 你的理解完全正确：用户**按行输入整句歌词**（例如“我醉清风”）。在“逐字打轴”时，系统**自动按字符拆分**该行，并为每个字符记录时间戳。这要求中文歌词按字拆分，英文可按单词拆分，但初期可按字符处理。

8.  **`.krc` 文件格式**：
    - 完全依赖 `@jyostudio/lyric` 库进行处理。我们不需要深入了解其二进制加密或压缩细节，只需调用库的读写接口即可。

9.  **LRC 导出的细节**：
    - 支持**标准LRC格式**：`[mm:ss.xx]歌词内容`。
    - 同时支持**扩展元数据标签**：`[ti:]`(标题)、`[ar:]`(作者)、`[al:]`(专辑)。这些元数据可以在界面上提供输入框让用户填写。

10. **窗口样式**：
    - 第一阶段（MVP），使用 **Electron 标准窗口样式**（即有原生标题栏），这样可以减少早期复杂度，让你能专注于核心歌词制作逻辑。未来迭代时再考虑自定义标题栏。

### 关于开发阶段

11. **第一阶段的目标**：
    - 在“搭建完整的项目目录结构和基础框架代码”的基础上，**需要包含一个最小可运行的UI界面**。具体来说，第一阶段交付应包含：
        1.  ✅ 项目初始化、依赖安装（`pnpm`），并确保 `pnpm dev` 能成功启动Electron应用。
        2.  ✅ 主进程与渲染进程的骨架代码。
        3.  ✅ 基础的IPC通信通道（如 `dialog:openFile` 用于打开文件）。
        4.  ✅ React + TypeScript + Ant Design的渲染进程框架。
        5.  ✅ Zustand Store的骨架（包含 `lyricData`、`playState` 等核心状态）。
        6.  ✅ 核心服务接口定义（`AudioEngine`、`LyricEngine`、`FileService` 的类或模块，包含方法签名）。
        7.  🎯 **一个简单的UI布局**：顶部工具栏（打开文件、模式切换），中间歌词列表（可显示文字），底部音频控制条（播放/暂停、进度条）。
    - **目标**：这个阶段完成后，应用应该能启动、能通过“打开”对话框加载一个音频文件并播放，歌词列表能显示静态文本。


