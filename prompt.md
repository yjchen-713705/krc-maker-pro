好的，以下是根据你要求细化的两个功能开发 Prompt，你可以直接提供给 AI 编程助手：

---

## 功能一：应用菜单（Menu Bar）

### 📋 功能目标

为应用添加完整的原生菜单栏，在现有"视觉"菜单基础上扩展，包含以下核心菜单项：

| 菜单 | 子菜单项 | 功能描述 |
|------|----------|----------|
| **文件** | 修改默认保存路径 | 打开文件夹选择对话框，设置默认保存路径，后续保存文件时自动填入该路径，同时保留“浏览”按钮供用户临时更改 |
| **快捷键** | 快捷键列表与自定义 | 显示所有可用快捷键列表，支持用户自定义快捷键，不允许重复绑定 |
| **视觉** | 白天/黑夜模式 + 主色调选择 | 已实现，需在菜单中保留入口 |

---

### 🔧 详细实现要求

#### 1. 文件 → 修改默认保存路径

**功能描述**：

- 菜单项名称为“修改默认保存路径…”
- 点击后打开系统文件夹选择对话框（Electron `dialog.showOpenDialog` 或 `dialog.showSaveDialog` 的目录选择模式）
- 用户选择一个目录后，将该路径保存到 `localStorage`（key: `defaultSavePath`）
- 后续导出 KRC/LRC 文件时：
  - 保存对话框的默认路径 = `defaultSavePath`（如果存在）
  - 同时提供“浏览”按钮（`dialog.showSaveDialog` 的 `defaultPath` 属性），允许用户临时选择其他路径
- 如果用户从未设置过默认路径，保存对话框的默认路径 = 用户的“下载”目录或项目根目录

**技术实现要点**：

- 主进程负责打开文件夹选择对话框，通过 IPC 返回选中的路径
- 主进程启动时读取 `localStorage` 中的 `defaultSavePath`，注入到保存对话框的 `defaultPath`
- 渲染进程通过 `FileService` 调用主进程的 IPC

**菜单结构**：

```
文件
  ├── 修改默认保存路径…
  ├── ────────────────
  └── 退出 (Cmd+Q)
```

---

#### 2. 快捷键 → 快捷键列表与自定义

**功能描述**：

- 菜单项名称为“快捷键设置…”
- 点击后打开一个模态框（Modal），显示当前所有可用的快捷键列表
- 每个快捷键项显示：**功能名称** + **当前快捷键组合**
- 用户可点击任意快捷键组合，按下新的组合键进行修改
- 禁止重复绑定：如果用户输入的组合键已被其他功能占用，给出提示“该快捷键已被 [功能名] 占用，请重新设置”
- 自定义后的快捷键保存到 `localStorage`（key: `customShortcuts`），重启应用后恢复
- 快捷键配置需在应用启动时应用到 `useKeyboardShortcuts` 的监听逻辑中

**快捷键列表（默认配置）**：

| 功能名称 | 默认快捷键 (Windows/Linux) | 默认快捷键 (macOS) |
|----------|---------------------------|-------------------|
| 播放/暂停 | `Space` | `Space` |
| 标记当前字（逐字打轴） | `→` (Right Arrow) | `→` (Right Arrow) |
| 标记当前行（逐句打轴） | `Enter` | `Enter` |
| 选择上一行 | `↑` (Up Arrow) | `↑` (Up Arrow) |
| 选择下一行 | `↓` (Down Arrow) | `↓` (Down Arrow) |
| 撤销 | `Ctrl+Z` | `Cmd+Z` |
| 重做 | `Ctrl+Y` | `Cmd+Y` |
| 关闭预览 | `ESC` | `ESC` |

**技术实现要点**：

- 快捷键监听逻辑从 `localStorage` 读取自定义配置，覆盖默认配置
- 检测重复绑定的逻辑：维护一个 `{ shortcut: functionName }` 的映射表，用户输入组合键时查表
- 快捷键输入组件：监听 `keydown` 事件，记录组合键（`ctrlKey`、`shiftKey`、`altKey`、`metaKey` + `key`），显示为标准格式（如 `Ctrl+Shift+Z`）
- 如果用户清空某个快捷键（按 `Backspace` 或 `Delete`），恢复为默认值

**菜单结构**：

```
快捷键
  ├── 快捷键设置…
  └── ────────────────
      (列出所有功能的当前快捷键，只读显示)
```

> **说明**：第二项是只读列表，方便用户快速查看所有快捷键，不带自定义功能。自定义功能在“快捷键设置…”弹窗中完成。

---

#### 3. 视觉菜单（保持现有）

将已实现的主题切换功能通过菜单暴露，方便用户快速切换（与工具栏的设置入口并行）。

**菜单结构**：

```
视觉
  ├── 白天模式
  ├── 黑夜模式
  ├── ────────────────
  └── 主色调 → 极客蓝
               科技靛蓝
               极光紫
               霓虹粉
               赛博青
               活力橙
               抹茶绿
               中性灰
```

---



## 功能二：LRC 元数据编辑界面

### 📋 功能目标

在菜单栏中新增“编辑”菜单，提供两个核心功能：

1. **元数据编辑**：弹窗编辑歌曲的元数据（标题、歌手、专辑、制作人等）
2. **批量编辑歌词文本**：弹窗中显示所有歌词行的纯文本内容，支持批量修改，修改后自动重新拆分字（保留已有时间戳）

菜单位置：在现有顶部“打轴模式”菜单的左侧。

```
菜单栏结构：
文件  |  编辑  |  打轴模式  |  视觉  |  快捷键  |  帮助
         ├── 元数据编辑…
         └── 批量编辑歌词文本…
```

---

### 🔧 详细实现要求

#### 1. 元数据编辑

**功能描述**：

- 菜单项名称为“元数据编辑…”
- 点击后弹出一个模态框（Modal），包含以下输入字段：

| 字段名 | 对应的元数据 Key | 说明 |
|--------|-----------------|------|
| 歌曲标题 | `[ti:]` | 输入框 |
| 歌手 | `[ar:]` | 输入框 |
| 专辑 | `[al:]` | 输入框 |
| 制作人/作者 | `[au:]` | 输入框（可选） |
| 歌词制作 | `[by:]` | 输入框（可选） |

- 点击“保存”按钮，将字段值写入 `lyricData.metadata`
- 点击“取消”或关闭按钮，关闭弹窗
- 如果 `lyricData` 为空（未导入任何歌词），提示“请先导入或粘贴歌词”

**技术实现要点**：

- 使用 Ant Design 的 `Modal` + `Form` + `Input` 组件
- 元数据存储在 `lyricData.metadata` 对象中，格式为 `Record<string, string>`
- 导出 KRC/LRC 时，`toLrcText` 从 `metadata` 读取并输出 `[ti:]` 等标签

---

#### 2. 批量编辑歌词文本

**功能描述**：

- 菜单项名称为“批量编辑歌词文本…”
- 点击后弹出一个模态框（Modal），包含一个大的 `TextArea`
- 文本框中按行显示当前所有歌词行的纯文本内容（不含时间戳）
  - 格式：每行一句歌词，按 `\n` 分隔
- 用户可自由修改文本内容（增删改行）
- 点击“保存”按钮：
  - 按换行符拆分文本为行数组
  - 用新文本替换 `lyricData.lines` 中的每一行的 `text` 字段
  - **保留已有时间戳**：如果某行的 `startTime` 存在，保留该值；如果文本行数发生变化（新增或删除行），对应关系按索引位置映射
    - 新行数 > 旧行数：新增的行 `startTime = 0`（未标记）
    - 新行数 < 旧行数：超出部分的旧行丢弃
  - 重新执行 `splitLineIntoWords` 拆分每行文字为字数组，**原有字的 `startTime` 尽量保留**（如果文本顺序未变，按位置匹配）
  - 关闭弹窗，刷新歌词列表
- 点击“取消”或关闭按钮，放弃修改

**技术实现要点**：

- 文本行数变化时的时间戳保留策略：
  - 按索引逐行比对：如果新行文本与旧行文本完全一致，保留该行的所有时间戳
  - 如果某行文本发生变化，重置该行的所有字的时间戳为 0
  - 如果行数变化，新增行的时间戳为 0，超出部分丢弃
- 使用 `TextArea` 的 `autoSize` 属性，高度自适应

**边界情况**：

| 场景 | 处理方式 |
|------|----------|
| 用户未导入任何歌词 | 弹窗显示空 TextArea，提示“当前无歌词” |
| 修改后文本为空 | 保存时清空所有行，提示“歌词内容为空，确认保存？” |
| 修改后行数减少 | 丢弃多余行的数据（含时间戳） |
| 修改后行数增加 | 新增行的时间戳为 0，等待用户打轴 |

---


## 📝 技术约束与参考

- **菜单实现**：使用 Electron 原生 `Menu.buildFromTemplate`，在主进程中配置。菜单点击事件通过 `ipcMain` 发送消息到渲染进程，渲染进程通过 `window.electronAPI` 监听。
- **IPC 通道命名**：
  - `menu:open-folder-selector` → 打开文件夹选择对话框
  - `menu:open-metadata` → 打开元数据编辑弹窗
  - `menu:open-batch-edit` → 打开批量编辑弹窗
- **快捷键设置**：修改快捷键后，`useKeyboardShortcuts` 中的 `keydown` 监听逻辑需要重新订阅（或将配置作为依赖项，通过 `useEffect` 重新注册）。
- **样式**：延续现有 Ant Design 设计风格，元数据编辑弹窗和批量编辑弹窗的样式与现有 Modal 保持一致。
- **持久化**：元数据存储在 `lyricData` 中，随项目保存（如有项目文件）；快捷键配置存储在 `localStorage` 中，独立于项目。

---

## ✅ 验收标准

### 文件菜单

| 测试项 | 预期结果 |
|--------|----------|
| 修改默认保存路径 | 弹出文件夹选择对话框，选择后路径保存到 localStorage |
| 导出文件 | 保存对话框默认路径 = 用户设置的默认路径 |
| 退出 | 应用关闭 |

### 快捷键菜单

| 测试项 | 预期结果 |
|--------|----------|
| 打开快捷键设置弹窗 | 显示所有快捷键列表 |
| 修改某个快捷键 | 按下新组合键，更新显示，保存到 localStorage |
| 输入重复快捷键 | 提示“该快捷键已被 [功能名] 占用” |
| 按 Backspace 清空 | 恢复为默认快捷键 |
| 重启应用 | 快捷键配置恢复 |

### 编辑菜单

| 测试项 | 预期结果 |
|--------|----------|
| 元数据编辑 | 输入内容后保存，导出 LRC 时包含对应的 `[ti:]` 等标签 |
| 批量编辑歌词 | 修改文本后保存，歌词列表刷新，已有时间戳按规则保留 |
| 无歌词时打开编辑 | 提示“请先导入或粘贴歌词” |
